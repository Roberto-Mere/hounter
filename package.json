{
  "name": "hounter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "watch:sass": "sass src/sass/main.scss src/css/style.css --watch",
    "compile:sass": "sass src/sass/main.scss src/css/style.css",
    "start-json": "json-server --watch src/js/db.json --port 5000"
  },
  "devDependencies": {
    "sass": "^1.77.8",
    "vite": "^5.4.1"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.2"
  }
}
